{
  "name": "watchtogether",
  "version": "1.0.0",
  "private": true,
  "description": "A free React + Node watch party prototype with room keys and synchronized YouTube playback.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js",
    "smoke": "node --experimental-websocket scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
